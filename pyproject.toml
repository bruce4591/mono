[project]
name = "market-mvp"
version = "0.1.0"
description = "Lightweight multi-market data, ranking, and charting system."
requires-python = ">=3.11"
dependencies = [
    "akshare>=1.18,<2",
    "psycopg[binary]>=3.2,<4",
    "websocket-client>=1.8,<2",
]

[project.scripts]
market = "market.cli:main"
